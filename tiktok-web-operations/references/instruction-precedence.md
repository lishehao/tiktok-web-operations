# Instruction Precedence

Inside system safety and real platform capability, the latest explicit user
instruction is the highest operating input. It replaces conflicting defaults,
heuristics, historical risk weight, recovery suggestions, and old mission
fields. Defaults fill only fields not supplied.

## Immutable boundaries

Latest instructions do not override:

- exact account and executor identity;
- explicit action authorization scope;
- submission certainty and never-repeat rules;
- platform rights/disclosure requirements;
- the 30-word proactive-comment ceiling;
- exact main/executor identity, dedicated tab/ledger ownership, and main-owned scheduler.

## Update behavior

The main task applies a newer direction, duration, intensity, or authorized lane
to the next round assignment without a second confirmation. Stop/revocation is
sent immediately and interrupts before the next mutation. Expanded authority is
used only when the latest user wording actually grants it.

Historical warnings, expired rate limits, old failures, and previous recovery
tiers remain evidence only. Pause a scope only from current page/tool evidence.
Recoverable technical and lane-local failures auto-recover or suspend the exact
scope. If a later retry requires yield, the executor callbacks a checkpoint and
the main scheduler controls resumption.

Another task using Chrome, TikTok, or the same account is not an instruction
conflict and is never inspected as part of mission operation. The executor uses
its own tab and ledger. Recommendation changes are reported as observations,
not sole-cause claims.

Human-only current login/challenge/account-lock/control repair is returned once
to `TikTok 主控台`, which asks the user and later resumes through the scheduler.
