interface:
  display_name: "TikTok Web Operations"
  short_description: "Preflight, choose direction, then run TikTok"
  default_prompt: "Use $tiktok-web-operations with $thread-supervisor: keep TikTok 启动台 as a reusable stateless fresh-only dispatcher; every new operating command creates one new independent TikTok 执行台 without inspecting historical tasks, then returns idle. The executor owns its Chrome tab, ledger, recovery, self-Heartbeat, user reports, and 35-target/25–45-view rounds."
