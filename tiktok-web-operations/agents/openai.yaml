interface:
  display_name: "TikTok Web Operations"
  short_description: "Preflight, choose direction, then run TikTok"
  default_prompt: "Use $tiktok-web-operations with $thread-supervisor: keep TikTok 启动台 as a reusable stateless fresh-only dispatcher. Each new TikTok 执行台 owns 35-target/25–45-view rounds and evaluates independently gated Like, Favorite, Repost, and short Comment actions while each qualified video is open, without a separate interaction phase."
