interface:
  display_name: "TikTok Web Operations"
  short_description: "Preflight, choose direction, then run TikTok"
  default_prompt: "Use $tiktok-web-operations with $thread-supervisor: keep TikTok 启动台 temporary for setup/repair, then rename the same healthy task to pinned TikTok 分发台 for fresh-only dispatch. Each unpinned executor runs 35-target rounds, then takes a 10–20 minute cooldown using its one persistent Heartbeat."
