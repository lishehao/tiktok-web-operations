interface:
  display_name: "TikTok Web Operations"
  short_description: "Preflight, choose direction, then run TikTok"
  default_prompt: "Use $tiktok-web-operations with $thread-supervisor: finish preflight, make this task TikTok 主控台, create one TikTok 执行台, and run one continuous resumable search-led mission until operation_stop_at while keeping correctly bound Heartbeats active through recoverable failures."
