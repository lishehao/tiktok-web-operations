interface:
  display_name: "TikTok Web Operations"
  short_description: "Preflight, choose direction, then run TikTok"
  default_prompt: "Use $tiktok-web-operations to run read-only preflight, ask for direction and duration, then start the two persistent Luna/High TikTok Threads after I answer."
