interface:
  display_name: "Thread Supervisor"
  short_description: "Assign and schedule persistent tasks safely"
  default_prompt: "Use $thread-supervisor to select the calling domain's explicit topology, verify exact task IDs, and enforce either coordinator-worker ownership or one-way launcher-to-self-owned-executor assignment with exact Heartbeat binding."
