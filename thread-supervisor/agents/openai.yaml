interface:
  display_name: "Thread Supervisor"
  short_description: "Manage, fork, hand off, and supervise Codex tasks"
  default_prompt: "Use $thread-supervisor to manage this Codex task or coordinate related persistent tasks with exact IDs, minimal polling, and verified lifecycle actions."
