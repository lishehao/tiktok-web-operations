interface:
  display_name: "Thread Supervisor"
  short_description: "Coordinate persistent tasks with callbacks"
  default_prompt: "Use $thread-supervisor to verify this task's identity, coordinate one persistent executor, and bind any heartbeat only to this exact coordinator."
