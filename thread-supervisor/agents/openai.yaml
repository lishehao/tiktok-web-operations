interface:
  display_name: "Thread Supervisor"
  short_description: "Coordinate persistent tasks with callbacks"
  default_prompt: "Use $thread-supervisor to verify exact coordinator/executor IDs, apply caller-supplied titles and pin policy (or generic defaults), and bind any heartbeat only to the exact coordinator."
