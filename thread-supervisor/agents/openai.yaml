interface:
  display_name: "Thread Supervisor"
  short_description: "Coordinate persistent tasks with callbacks"
  default_prompt: "Use $thread-supervisor to verify exact coordinator/executor IDs, apply caller-supplied titles and pin policy (or generic defaults), and let only the verified coordinator manage heartbeats whose targets match the calling domain's declared topology."
