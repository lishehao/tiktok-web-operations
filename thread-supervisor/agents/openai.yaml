interface:
  display_name: "Thread Supervisor"
  short_description: "Assign and schedule persistent tasks safely"
  default_prompt: "Use $thread-supervisor to enforce exact task identity, any same-task bootstrap-to-pinned-distributor transition, explicit pre-dispatch gates, and fresh-only one-way executor assignment with a self-owned recurring Heartbeat."
