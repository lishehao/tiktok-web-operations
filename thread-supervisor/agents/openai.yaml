interface:
  display_name: "Thread Supervisor"
  short_description: "Assign and schedule persistent tasks safely"
  default_prompt: "Use $thread-supervisor to enforce the calling domain's topology and explicit pre-dispatch gates. A reusable stateless launcher must obtain exact gate exit proof before every fresh-only one-way executor assignment, with no historical-task reuse or worker return."
