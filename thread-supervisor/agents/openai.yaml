interface:
  display_name: "Thread Supervisor"
  short_description: "Coordinate persistent tasks with callbacks"
  default_prompt: "Use $thread-supervisor to give this coordinator and its executor stable role/scope/nonce titles, verify their identities, and bind any heartbeat only to this exact coordinator."
